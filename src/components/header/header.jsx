import React from "react";

const Header = () => {
  return (
    <header
      style={{
        display: "flex",
        alignItems: "center",
        flexDirection: "column"
      }}
    >
      <h3 style={{ margin: "0 0" }}>table</h3>
      <p>its a table</p>
    </header>
  );
};

export default Header;
